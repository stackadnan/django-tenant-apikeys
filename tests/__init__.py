"""Test suite for django-tenant-apikeys.

This package doubles as a minimal installed Django app (see
``INSTALLED_APPS`` in :mod:`tests.settings`) so that :mod:`tests.models` can
define concrete subclasses of the library's abstract models for exercising
in tests.
"""
